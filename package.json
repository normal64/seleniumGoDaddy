{
  "name": "selenuimtry27.03",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "set NODE_ENV=test&&  mocha --no-timeouts --parallel --reporter mochawesome --require mochawesome/register --reporter-option mocha 'test/**/*.js'",
    "test-goDaddy": "set NODE_ENV=test && mocha --no-timeouts --reporter mochawesome --require mochawesome/register test/goDaddy.js",
    "test-goDaddyLink": "set NODE_ENV=test && mocha --no-timeouts --reporter mochawesome --require mochawesome/register test/goDaddyLink.js",
    "test-goDaddyAllLinks": "set NODE_ENV=test && mocha --no-timeouts --reporter mochawesome --require mochawesome/register test/goDaddyAllLinks.js",
    "test-goDaddyDomainName": "set NODE_ENV=test && mocha --no-timeouts --reporter mochawesome --require mochawesome/register test/goDaddyDomainName.js",
    "test-goBrokenLinks": "set NODE_ENV=test && mocha --no-timeouts --reporter mochawesome --require mochawesome/register test/brokenLinks.js",
    "test-practiceForm": "set NODE_ENV=test && mocha --no-timeouts --reporter mochawesome --require mochawesome/register test/practiceForm.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.3.5",
    "chai": "^4.3.7",
    "chromedriver": "^111.0.0",
    "chromedriver-version-matcher": "^1.1.9",
    "mocha": "^10.2.0",
    "mochawesome": "^7.1.3",
    "puppeteer": "^19.8.0",
    "selenium-webdriver": "^4.8.2"
  }
}
